name: schedule-tests

on:
  schedule:
    - cron: "0 7 * * *"   # daily at 07:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/ci.yaml
